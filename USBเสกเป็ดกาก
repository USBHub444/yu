local P, CG, TS, RS, SS = game:GetService("Players"), game:GetService("CoreGui"), game:GetService("TweenService"), game:GetService("ReplicatedStorage"), game:GetService("SoundService") local LP, isDuping = P.LocalPlayer, false if CG:FindFirstChild("OptimizedDuckHub") then CG.OptimizedDuckHub:Destroy() end local snd = Instance.new("Sound", SS) snd.SoundId, snd.Volume = "rbxassetid://138567614125924", 2 task.spawn(function() while task.wait(1) do pcall(function() LP.PlayerGui.TouchGui.Enabled = true LP.PlayerGui.TouchGui.TouchControlFrame.Visible = true end) end end) local function getCount() local c = 0 for _, container in ipairs({LP:FindFirstChild("Backpack"), LP.Character}) do if container then for _, item in ipairs(container:GetChildren()) do if item:IsA("Tool") and item.Name == "Duck" then c = c + 1 end end end end return c end local gui = Instance.new("ScreenGui", CG:FindFirstChildOfClass("ScreenGui") and CG or LP:WaitForChild("PlayerGui")) gui.Name, gui.ResetOnSpawn = "OptimizedDuckHub", false local main = Instance.new("Frame", gui) main.Size, main.Position, main.BackgroundColor3, main.Active, main.Draggable = UDim2.new(0, 200, 0, 150), UDim2.new(0.5, -100, 0.3, 0), Color3.fromRGB(15, 18, 28), true, true Instance.new("UICorner", main).CornerRadius = UDim.new(0, 12) Instance.new("UIStroke", main).Color = Color3.fromRGB(0, 200, 255) local title = Instance.new("TextLabel", main) title.Size, title.BackgroundColor3, title.Text, title.TextColor3, title.Font, title.TextSize = UDim2.new(1, 0, 0, 32), Color3.fromRGB(25, 28, 42), "Duck HUB", Color3.fromRGB(255, 170, 0), Enum.Font.FredokaOne, 16 Instance.new("UICorner", title).CornerRadius = UDim.new(0, 12) local function makeBtn(pos, text) local b = Instance.new("TextButton", main) b.Size, b.Position, b.BackgroundColor3, b.Text, b.TextColor3, b.Font, b.TextSize, b.AutoButtonColor = UDim2.new(0.88, 0, 0, 42), pos, Color3.fromRGB(0, 120, 230), text, Color3.fromRGB(255, 255, 255), Enum.Font.GothamBold, 16, false Instance.new("UICorner", b).CornerRadius = UDim.new(0, 8) local s = Instance.new("UIStroke", b) s.Thickness, s.Color = 2, Color3.fromRGB(0, 220, 255) local ts = Instance.new("UIStroke", b) ts.ApplyStrokeMode, ts.Thickness, ts.Color = Enum.ApplyStrokeMode.Contextual, 2.5, Color3.fromRGB(0, 0, 0) return b, s end local btnDup, strokeDup = makeBtn(UDim2.new(0.06, 0, 0.28, 0), "🔥 ดูป 10 อัน") local btnSpawn = makeBtn(UDim2.new(0.06, 0, 0.62, 0), "🦆 เสกทั้งหมด") task.spawn(function() while true do task.wait() if isDuping and LP.Character and LP.Character:FindFirstChildOfClass("Humanoid") then pcall(function() LP.Character:FindFirstChildOfClass("Humanoid"):UnequipTools() end) end end end) btnDup.MouseButton1Click:Connect(function() pcall(function() snd:Play() end) if isDuping or getCount() >= 10 then return end isDuping = true strokeDup.Color = Color3.fromRGB(80, 255, 160) task.spawn(function() local ts = RS:WaitForChild("ToolStorage", 5) if ts then ts = ts:WaitForChild("ToolsStorage", 5) end while isDuping do if getCount() >= 10 then isDuping = false strokeDup.Color = Color3.fromRGB(0, 220, 255) break end for i = 1, 100 do if not isDuping or getCount() >= 10 then break end for _, v in ipairs(workspace:GetDescendants()) do if v:IsA("ClickDetector") and v.Parent and v.Parent:IsA("BasePart") and (v.Parent.Position - Vector3.new(20.07, 178.07, 298.61)).Magnitude < 0.5 then if fireclickdetector then fireclickdetector(v) end break end end task.wait() if ts then pcall(function() ts:FireServer("Save", "Duck") end) end end task.wait() for i = 1, 1000 do if not isDuping or getCount() >= 10 then break end if ts then pcall(function() ts:FireServer("Get", "Duck") end) end task.wait() end task.wait() end end) end) btnSpawn.MouseButton1Click:Connect(function() pcall(function() snd:Play() end) local char, bp = LP.Character, LP:FindFirstChild("Backpack") if char and bp then for _, item in ipairs(bp:GetChildren()) do if item:IsA("Tool") and item.Name == "Duck" then for _, r in ipairs(item:GetDescendants()) do if r:IsA("RemoteEvent") then pcall(function() r:FireServer() end) elseif r:IsA("RemoteFunction") then pcall(function() r:InvokeServer() end) end end item.Parent = char item:Activate() item.Parent = bp end end end end)
